apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kfleet-agent.fullname" . }}
  labels:
    {{- include "kfleet-agent.labels" . | nindent 4 }}
type: Opaque
data:
  hub-token: {{ required "hub.token is required; set it to a hub registration token" .Values.hub.token | b64enc | quote }}

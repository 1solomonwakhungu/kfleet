# kfleet landing page

The public marketing site for kfleet, built with React, Vite, Tailwind CSS v4, and the Better Design Linear component system.

```bash
npm install
npm run dev
```

Build the production bundle with `npm run build`. Vercel should use `landing-page` as the project root.
